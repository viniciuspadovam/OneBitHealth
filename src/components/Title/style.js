import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 3,
  },
  title: {
    color: '#ad1717',
    fontSize: 24,
    fontWeight: 'bold',
    textShadowColor: '#ad1717',
    textShadowOffset: { width: 0, height: 0 },
    textShadowRadius: 6,
  },
});

export default styles;
